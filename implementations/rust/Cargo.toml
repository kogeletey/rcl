[package]
name = "rcl-parser"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "RCL parser and formatter"

[lib]
name = "rcl_parser"
path = "src/lib.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_yaml = "0.9"
toml = "0.8"
